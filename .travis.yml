language: dart

dart:
  - dev

dart_task:
  - test: --platform vm
  # No parallelism on Firefox (-j 1)
  # Causes flakiness – need to investigate
  - test: --platform firefox -j 1
  - dartanalyzer
  - dartfmt

cache:
  directories:
    - $HOME/.pub-cache
